import 'package:flutter/material.dart';

class DetailScreen14 extends StatelessWidget {
  const DetailScreen14({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Außenlager Hamburg-Hammerbrook')),
      body: const Center(
        child: Text(
          'Außenlager Hamburg-Hammerbrook',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
