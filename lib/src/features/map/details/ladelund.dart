import 'package:flutter/material.dart';

class DetailScreen15 extends StatelessWidget {
  const DetailScreen15({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Außenlager Ladelund')),
      body: const Center(
        child: Text('Außenlager Ladelund', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
