import 'package:flutter/material.dart';

class DetailScreen16 extends StatelessWidget {
  const DetailScreen16({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('KZ Pavlos Melas')),
      body: const Center(
        child: Text('KZ Pavlos Melas', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
