import 'package:flutter/material.dart';

class DetailScreen7 extends StatelessWidget {
  const DetailScreen7({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('KZ Mauthausen')),
      body: const Center(
        child: Text('KZ Mauthausen', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
