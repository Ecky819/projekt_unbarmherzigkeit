import 'package:flutter/material.dart';

class DetailScreen8 extends StatelessWidget {
  const DetailScreen8({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('KZ Ravensbrück')),
      body: const Center(
        child: Text('KZ Ravensbrück', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
