import 'package:flutter/material.dart';

class DetailScreen9 extends StatelessWidget {
  const DetailScreen9({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('KZ Sachsenhausen')),
      body: const Center(
        child: Text('KZ Sachsenhausen', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
